# vim: syntax=zsh

cdpath=(~/Documents $cdpath)

if [[ `whence -p ls` == /opt/local/bin/ls ]]
then
	eval `dircolors`
	alias ls='ls --color=auto'
else
	export CLICOLOR=1
	export LSCOLORS="exfxcxdxbxgxdxBxGxCxDx"
fi

if [[ ! -n $SSH_CONNECTION ]]; then
	export EDITOR='mvim'
	export VISUAL='mvim'
fi
