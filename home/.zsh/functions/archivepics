function archivepics ()
{
	if [[ $# -eq 1 ]]
	then
	for dir in `find $1 -name images -type d | xargs echo`
		do
			name=`ls $dir | head -1 | sed -e 's/^[0-9]* //' -e 's/.[a-zA-Z0-9]*$//'`
			zip "$name.zip" $dir/*
		done
	fi
}

# vim:syntax=sh
