function zshsync ()
{
	if [[ $# -gt 0 ]]
	then
		for server in $*
		do
			rsync -avz .zaliases .zcomprc .zpath .zshenv .zshrc .zfunc .zkbd .screenrc .vimrc .Xdefaults $server:
		done
	else
		echo "usage: zshsync [ host ... ]"
	fi
}

# vim:syntax=sh
