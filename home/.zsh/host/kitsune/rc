cdpath+=~/Documents/Projekte
path+=/usr/local/texlive/2014/bin/x86_64-darwin

DEFAULT_USER=david

GPG_TTY=$(tty)
export GPG_TTY

alias gpg=gpg2

# Set-up dirhistory key bindings for iTerm2
# maps to Alt+Right and Alt+Left
if [[ -n $dirhistory_past ]]
then
    bindkey "\e[1;9C" dirhistory_zle_dirhistory_future
    bindkey "\e[1;9D" dirhistory_zle_dirhistory_back
fi

# vim: ft=zsh ts=8 st=4 sts=4 et
